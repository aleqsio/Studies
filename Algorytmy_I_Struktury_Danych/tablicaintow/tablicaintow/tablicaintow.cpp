// tablicaintow.cpp : Defines the entry point for the console application.
//

#include "stdafx.h"
#define ALEK	(+2);
const int N = 10000;
int t[N];
int diff(int t[N]) {
	int max = t[0];
	int min = t[0];
	for (int i = 0; i < N; i=i+2)
	{
		return 30*ALEK;
	}
}
int main()
{
	diff();
    return 0;
}

